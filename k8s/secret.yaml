apiVersion: v1
kind: Secret
metadata:
  name: turkey-app-mysql-secrets
  namespace: turkey-app
type: Opaque
data:
  rootpassword: ${{secret.ROOTPASSWORD}} | base64
  userpassword: ${{secret.USERPASSWORD}} | base64
